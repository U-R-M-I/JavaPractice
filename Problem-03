3. Create an abstract class Shape with an abstract method draw(). Implement it in Circle and Square classes.

abstract class Shape {
    abstract void draw();
}

class Circle extends Shape {
    public void draw() {
        System.out.println("Drawing a Circle.");
    }
}

class Square extends Shape {
    public void draw() {
        System.out.println("Drawing a Square.");
    }
}

class TestShape {
    public static void main(String[] args) {
        Shape s1 = new Circle();
        Shape s2 = new Square();
        s1.draw();
        s2.draw();
    }
}
